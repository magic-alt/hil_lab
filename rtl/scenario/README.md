# Scenario RTL

Target home for local event queues, sequencers, trigger logic and deterministic signal fault firing. The current dio_event_scheduler remains under rtl/io until migrated with all build references.
