# Timebase primitives

Canonical Architecture v2 home for reusable hardware timebase RTL.

Current source:

- `hil_timebase.v` — monotonic parameterized hardware tick counter used by FPGA HIL backends.

Legacy `rtl/time/hil_timebase.v` is forbidden by the architecture gate to prevent duplicate canonical sources.
