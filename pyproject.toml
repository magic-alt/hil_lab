[project]
name = "hil-lab-host"
version = "0.1.0"
description = "Backend-neutral host contract for hil_lab"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests/pytest"]
addopts = "-ra"
